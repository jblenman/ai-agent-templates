---
description: Run a code review on current changes or specified files
agent: code-reviewer
---
Review the code changes for quality, security, and correctness.

If no specific files are given, review all uncommitted changes (run `git diff` and `git diff --staged` to see what changed).

$ARGUMENTS
