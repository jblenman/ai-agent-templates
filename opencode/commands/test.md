---
description: Write and run tests for current changes or specified code
agent: qa-tester
---
Analyze the code and write appropriate tests. Follow existing test patterns in the project.

If no specific target is given, look at recent changes (`git diff` and `git diff --staged`) and write tests for the changed code.

$ARGUMENTS
