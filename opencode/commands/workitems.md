---
description: Query Azure DevOps work items, bugs, and tasks
agent: devops
---
Load the azure-devops-api skill for REST API reference. Then query Azure DevOps for the requested work items.

If no specific query is given, get my active work items (assigned to @me, not Closed/Removed) sorted by priority.

$ARGUMENTS
