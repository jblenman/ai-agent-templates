---
description: Generate documentation for code, changes, or architecture
agent: scribe
---
Generate documentation for the specified code or changes.

If no specific target is given, document the most recent changes — read `git log --oneline -10` and `git diff` to understand what changed, then produce appropriate documentation.

$ARGUMENTS
